Credits to:
- https://github.com/itgmania/itgmania/pull/570

Lua module from:
- https://github.com/CrashCringle12/Simply-Love-SM5/blob/discord-presence/Modules/DiscordPresence.lua

```sh
cd ./extern/discord-rpc-2.0.1 && \
ln -s "$PWD/DiscordPresence.lua" "$(dirname "$(dirname "$PWD")")/Themes/zmod-git/Modules/" && \
ln -s "$PWD/DiscordPresence.lua" "$(dirname "$(dirname "$PWD")")/Themes/Arrow Cloud 20251201/Modules/" && \
ln -s "$PWD/DiscordPresence.lua" "$(dirname "$(dirname "$PWD")")/Themes/Simply Love/Modules/"
```
